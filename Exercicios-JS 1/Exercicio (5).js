/* 5) Escrever um programa para exibir os números de 1 até 50 na tela. */

function numeros(){
    let nums = 1;
    while (nums <= 50) {
    console.log(`Número: ${nums}`);
    nums++
}
}

numeros();